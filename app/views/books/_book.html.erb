<h1>New book</h1>
  <%= form_with model: book,url: books_path,method: :post do |f| %>
    <h4 class="mb-3">Title</h4>
  <%= f.text_field :title %>
    <h4 class="mb-3">Opinition</h4>
  <%= f.text_area :body %><br>
  <%= f.submit 'Create Book', class: "btn btn-success mt-3"  %>
  <% end %>
