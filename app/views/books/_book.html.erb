<h1 class="mt-3">New book</h1>
      <%= form_with model: book,url: books_path,method: :post do |f| %>
      <h4>Title</h4>
      <%= f.text_area :title %>
      <h4>Opinition</h4>
      <%= f.text_field :body %>
      <%= f.submit 'Create Book' %>
      <% end %>
